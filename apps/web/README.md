# Web Dashboard

The sprint-one dashboard is a static HTML/CSS/JavaScript application served by the FastAPI app. It uses live API endpoints from `apps/api` and can later be replaced by a React or Next.js application without changing the backend contracts.

